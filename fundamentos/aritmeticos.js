const [a, b, c, d] = [3, 5, 1, 15]  // jeito facil de criar varias variaveis

const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2  //mostra o resto 0 ou 1

console.log(soma)
console.log(soma, subtracao, multiplicacao, divisao, modulo)
